port = 8000;

static = {
    document_root = "tests/static";
};

cgi = {
    document_root = "tests/cgi-bin";
    handlers = ( { handler = "adder.py"; url = "adder"; } );
};

dirt = {
    document_root = "tests/dirt";
    handlers = ( { library = "adder.so"; handler = "adder"; url = "dirty-adder"; } );
};
